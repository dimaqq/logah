sync:
    build: .
    command: python naive.py
    environment:
        - DEBUG
    volumes:
        - /var/run/docker.sock:/var/run/docker.sock
        - ./naive.py:/naive.py
test:
    build: .
    command: date
